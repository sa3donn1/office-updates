# نظام الحضور والانصراف - دليل الاستخدام

## نظرة عامة
تم تطوير نظام متكامل للحضور والانصراف في مشروعك يتضمن:
- تسجيل حضور وانصراف الموظفين
- إدارة السجلات من قبل الأدمن
- تصدير البيانات إلى Excel
- واجهة سهلة الاستخدام مع تحديثات فورية

---

## 1️⃣ المكونات المضافة

### قاعدة البيانات

#### جدول Attendance الجديد
```
attendance (الحضور والانصراف)
├── id (مفتاح أساسي)
├── employee_id (مفتاح أجنبي)
├── date (تاريخ يومي)
├── check_in (وقت الحضور)
├── check_out (وقت الانصراف)
├── notes (ملاحظات اختيارية)
```

**الميزات:**
- قيد فريد على (employee_id, date) لمنع تسجيل الحضور مرتين في نفس اليوم
- حساب تلقائي لساعات العمل

---

### 2️⃣ APIs والـ Routes

#### 1. تسجيل الحضور
```
POST /attendance/check-in/<employee_id>
```
**المنطق:**
- البحث عن سجل لنفس الموظف واليوم
- إذا كان موجوداً وفيه check_in: ترجع خطأ "تم تسجيل الحضور بالفعل"
- إذا لم يكن موجوداً: إنشاء سجل جديد وتسجيل check_in

**الرد:**
```json
{
  "success": true,
  "message": "تم تسجيل الحضور بنجاح",
  "check_in_time": "09:30:45"
}
```

---

#### 2. تسجيل الانصراف
```
POST /attendance/check-out/<employee_id>
```
**المنطق:**
- البحث عن سجل اليوم
- إذا لم يكن موجوداً: ترجع خطأ "لم يتم تسجيل حضور"
- إذا كان check_out موجوداً: ترجع خطأ "تم تسجيل الانصراف بالفعل"
- تسجيل check_out بالوقت الحالي

**الرد:**
```json
{
  "success": true,
  "message": "تم تسجيل الانصراف بنجاح",
  "check_out_time": "17:45:30",
  "working_hours": 8.25
}
```

---

#### 3. الحصول على حالة الحضور
```
GET /attendance/status/<employee_id>
```
**الرد:**
```json
{
  "success": true,
  "data": {
    "checked_in": true,
    "checked_out": false,
    "check_in_time": "09:30:45",
    "check_out_time": null,
    "working_hours": null
  }
}
```

---

#### 4. قائمة الحضور (للأدمن)
```
GET /attendance/admin/list
```
**الفلترة:**
- `?employee=اسم_الموظف`
- `?date=2024-01-15`
- الجمع بين الفلاتر: `?employee=أحمد&date=2024-01-15`

**الميزات:**
- pagination (20 سجل بالصفحة)
- ترتيب حسب التاريخ الأحدث أولاً
- أزرار تعديل وحذف

---

#### 5. تصدير إلى Excel
```
GET /attendance/admin/export
```
**المعاملات:**
- نفس معاملات الفلترة في قائمة الحضور

**الملف الناتج:**
- اسم الملف: `attendance_report_YYYY-MM-DD_HH-MM-SS.xlsx`
- الأعمدة:
  - # (الرقم)
  - اسم الموظف
  - التاريخ
  - وقت الحضور
  - وقت الانصراف
  - ساعات العمل
  - ملاحظات

---

#### 6. تعديل السجل (للأدمن)
```
POST /attendance/admin/edit/<attendance_id>
```
**البيانات:**
- `check_in` (وقت الحضور بصيغة HH:MM)
- `check_out` (وقت الانصراف بصيغة HH:MM)
- `notes` (ملاحظات)

---

#### 7. حذف السجل (للأدمن)
```
POST /attendance/admin/delete/<attendance_id>
```

---

## 3️⃣ واجهة الموظف

### لوحة تحكم الموظف (Employee Dashboard)
موقع الملف: `src/templates/employee_dashboard.html`

**الميزات المضافة:**
1. **كارت جديد للحضور والانصراف:**
   - زر "تسجيل الحضور" (أخضر)
   - زر "تسجيل الانصراف" (أحمر)

2. **عرض الحالة:**
   - إذا لم يسجل: "لم تقم بتسجيل الحضور بعد"
   - بعد الحضور: "تم تسجيل الحضور في: HH:MM:SS"
   - بعد الانصراف: عرض الوقتين وساعات العمل

3. **تحديثات فورية:**
   - الحالة تتحدث فوراً بعد الضغط على الزر
   - رسالة Toast توضح النتيجة (نجاح/خطأ)
   - تحديث تلقائي كل دقيقة

---

## 4️⃣ واجهة الأدمن

### صفحة الحضور والانصراف
الرابط: `/attendance/admin/list`
موقع الملف: `src/templates/attendance/admin_list.html`

**الميزات:**
1. **شريط الفلترة:**
   - اختيار الموظف من قائمة منسدلة
   - تحديد التاريخ
   - زر البحث وزر مسح الفلاتر
   - زر "تحميل Excel"

2. **جدول السجلات:**
   - أعمدة: #, اسم الموظف, التاريخ, وقت الحضور, وقت الانصراف, ساعات العمل, الإجراءات
   - شارات ملونة للأوقات (أخضر للحضور، أحمر للانصراف)
   - أزرار تعديل (أصفر) وحذف (أحمر)

3. **Pagination:**
   - 20 سجل بالصفحة
   - أزرار التنقل بين الصفحات

---

### صفحة تعديل السجل
الرابط: `/attendance/admin/edit/<id>`
موقع الملف: `src/templates/attendance/edit.html`

**الميزات:**
- عرض بيانات الموظف والتاريخ (غير قابلة للتعديل)
- حقول إدخال أوقات الحضور والانصراف
- عرض الأوقات الحالية كمرجع
- حقل ملاحظات
- حساب ساعات العمل تلقائياً
- أزرار حفظ و إلغاء

---

## 5️⃣ التحقق من البيانات (Validation)

✅ **لا يوجد تسجيل حضور مرتين لنفس اليوم**
- قيد فريد في قاعدة البيانات على (employee_id, date)

✅ **لا يوجد تسجيل انصراف بدون حضور**
- التحقق من وجود check_in قبل تسجيل check_out

✅ **لا يوجد تسجيل انصراف مرتين**
- التحقق من عدم وجود check_out مسبقاً

✅ **الأدمن فقط يقدر يعدّل أو يمسح**
- التحقق من session["admin_id"] في جميع العمليات

---

## 6️⃣ الملفات المضافة والمعدلة

### ملفات جديدة:
```
✨ src/models/attendance.py             (نموذج الحضور)
✨ src/routes/attendance.py             (المسارات والـ APIs)
✨ src/templates/attendance/admin_list.html  (قائمة الحضور للأدمن)
✨ src/templates/attendance/edit.html   (تعديل السجل)
```

### ملفات معدلة:
```
📝 src/main.py                          (إضافة الـ import والـ blueprint)
📝 src/templates/employee_dashboard.html (إضافة أزرار الحضور والانصراف)
📝 src/templates/base.html              (إضافة الرابط في القائمة الجانبية)
```

### رزم جديدة مثبتة:
```
📦 openpyxl                             (لتصدير ملفات Excel)
```

---

## 7️⃣ الاستخدام

### للموظف:
1. قم بتسجيل الدخول
2. اذهب إلى لوحة تحكم الموظف
3. اضغط على زر "تسجيل الحضور" في الصباح
4. اضغط على زر "تسجيل الانصراف" في المساء
5. ستظهر رسالة تأكيد لكل إجراء

### للأدمن:
1. اذهب إلى "الحضور والانصراف" من القائمة الجانبية
2. استخدم الفلاتر للبحث عن سجلات معينة
3. اضغط "تحميل Excel" لتحميل التقرير
4. اضغط على أيقونة التعديل لتعديل سجل
5. اضغط على أيقونة الحذف لحذف سجل

---

## 8️⃣ معادلة حساب ساعات العمل

```
ساعات العمل = (check_out - check_in) / 3600 ثانية
```

مثال:
- حضور: 09:30:00
- انصراف: 17:45:30
- ساعات العمل: 8.258 ساعة ≈ 8 ساعات و15 دقيقة

---

## 9️⃣ مثال على ملف Excel المُصدَّر

| #  | اسم الموظف | التاريخ    | وقت الحضور | وقت الانصراف | ساعات العمل | ملاحظات      |
|----|-----------|-----------|-----------|------------|----------|------------|
| 1  | أحمد محمد | 2024-01-15 | 09:30:15  | 17:45:30   | 8.25     | -          |
| 2  | فاطمة علي | 2024-01-15 | 09:15:00  | 17:00:00   | 7.75     | تأخر بسيط  |
| 3  | محمود حسن | 2024-01-15 | 09:45:30  | -          | -        | لم يصرف بعد |

---

## 🔟 الأمان والصلاحيات

- **التحقق من تسجيل الدخول:** جميع المسارات تتطلب تسجيل دخول
- **صلاحيات الموظف:** لا يمكن تسجيل حضور/انصراف موظف آخر (إلا للأدمن)
- **صلاحيات الأدمن:** التعديل والحذف للأدمن فقط
- **الحماية من الأخطاء:** معالجة شاملة للأخطاء وإرجاع رسائل واضحة

---

## ملاحظات إضافية

✅ النظام يستخدم التوقيت المحلي للخادم
✅ جميع الرسائل بالعربية
✅ الواجهة مناسبة للهواتف (Responsive)
✅ دعم صيغة RTL (من اليمين لليسار)
✅ معالجة التاريخ والوقت تتم تلقائياً

---

## استكشاف الأخطاء الشائعة

### المشكلة: "ModuleNotFoundError: No module named 'openpyxl'"
**الحل:** تم تثبيت المكتبة بالفعل، تأكد من استخدام نفس بيئة Python

### المشكلة: "تم تسجيل الحضور بالفعل"
**السبب:** يوجد حضور للموظف اليوم
**الحل:** الانصراف سيتم فقط بعد محاولة الانصراف

### المشكلة: "لم يتم تسجيل حضور"
**السبب:** لا يوجد سجل حضور لهذا اليوم
**الحل:** يجب تسجيل الحضور أولاً

---

تم التطوير والاختبار بنجاح ✅
